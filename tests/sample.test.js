const sum = require('../library.js')

test('adds 1 + 2 to equal 3', () => {
    // Ejemplo de prueba exitosa
  expect(sum(1, 2)).toBe(3)
})

// test('adds 1 + 3 to equal 4', () => {
//     // Ejemplo de prueba que falla
//   expect(sum(1, 3)).toBe(3)
// })
